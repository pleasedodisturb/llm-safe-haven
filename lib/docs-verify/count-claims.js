'use strict';

// STUB (RED phase, G-1570, 21-04, Check 7). Parse-valid, deliberately wrong
// answers -- replaced by the real implementation before the GREEN commit.

const id = 'count-claims';

const CLAIM_REGISTRY = Object.freeze([]);
const SCOPED_DOCS = Object.freeze([]);
const CANONICAL_SOURCES = Object.freeze({});

function wordToNumber() {
  return null;
}

function run() {
  return [];
}

module.exports = { id, run, CLAIM_REGISTRY, SCOPED_DOCS, CANONICAL_SOURCES, wordToNumber };
