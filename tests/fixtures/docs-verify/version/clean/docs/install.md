# Fixture Install Guide (clean)

```bash
npx docs-verify-fixture-version-clean@2.5.0
```

Pin the version exactly as shown above for reproducible installs. (Control A: this claim
exactly matches this root's own `package.json` version `2.5.0`.)
