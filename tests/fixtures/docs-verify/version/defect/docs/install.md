# Fixture Install Guide (defect)

```bash
npx docs-verify-fixture-version-defect@2.5.1
```

Pin the version exactly as shown above for reproducible installs. (Planted defect: this
claim is `2.5.1`, one character off from this root's own `package.json` version `2.5.0`.)
