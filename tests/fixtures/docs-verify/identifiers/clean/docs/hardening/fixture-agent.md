# Fixture Agent Hardening (clean)

Edit the `REAL_IDENTIFIER` array to customize; also see `realFunction(x)`. Internally this
uses `UNEXPORTED_CONST`, declared in `fixture-lib.js` but deliberately not exported from it.
