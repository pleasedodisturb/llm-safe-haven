# Fixture MCP Security Doc (unresolved-dynamic)

Content here is irrelevant to the test — the detector's own unresolvable
dynamic interpolation forces an incomplete sweep before any comparison
against this table happens.

| Detector | Rule IDs | Severity |
|----------|----------|----------|
| `unresolved-detector` | `placeholder-thing` | medium |
